package delta.agriculture.reposy;

import delta.agriculture.domain.Exigible;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExibleRepository extends JpaRepository<Exigible, Long> {
}
